apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: ticketing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
      - name: booking-service
        image: ananthakkumar/new_ticket-booking-service:v1.0.0
        ports:
        - containerPort: 4003
        envFrom:
        - configMapRef:
            name: ticketing-config
        - secretRef:
            name: ticketing-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 4003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4003
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: ticketing
spec:
  selector:
    app: booking-service
  ports:
  - port: 4003
    targetPort: 4003
  type: ClusterIP
