apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: ticketing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
      - name: event-service
        image: ananthakkumar/new_ticket-event-service:v1.0.0
        ports:
        - containerPort: 4002
        envFrom:
        - configMapRef:
            name: ticketing-config
        - secretRef:
            name: ticketing-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: ticketing
spec:
  selector:
    app: event-service
  ports:
  - port: 4002
    targetPort: 4002
  type: ClusterIP
